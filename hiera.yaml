---
:backends: 
  - yaml
:yaml:
  :datadir: /etc/puppet/hieradata/bps
:hierarchy:
  - "%{::deployment_pattern}/hosts"
  - "%{::deployment_pattern}/%{::clientcert}"
  - database
  - datasources
  - common
