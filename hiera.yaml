---
:backends: 
  - yaml
:yaml:
  :datadir: /etc/puppet/hieradata/bps
:hierarchy:
  - "%{::deployment_pattern}/hosts"
  - "%{::deployment_pattern}/config"
  - "%{::deployment_pattern}/datasources"
  - "%{::deployment_pattern}/%{::clientcert}"
  - "%{::deployment_pattern}/database"
  - common
